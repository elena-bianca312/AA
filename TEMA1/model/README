// Copyright Fetcu Radu-Stefan 324CA 2020

Cozi de prioritate - etapa 2

	Fișierul etapa2.cpp conține definirea celor 2 structuri de date si aplicarea metodelor
reaizate in fisierele heap.h si treap.h.

	Fisierul heap.h realizeaza o clasa de tipul MinHeap ce implementeaza metodele
MinHeap(constructor), insertKey(inserarea), extractMin(extragerea si stergerea elementului minim),
MinHeapify (o reordonare a vectorului), printHeap (functie De printare), getMin(intoarcere fara
stergere a elementului minim), swap.

	Fisierul treap.h implementeaza o structura de TreapNode ce rezolva metodele
rotateLeft/Right (rotirile realizate pentru asigurarea corectitudinii structurii),
insertNode(inserare), getMin(intoarcerea minimului), deleteNode(sterge un nod dupa
valoarea primita ca parametru daca este gasit), printTreap(o printare in inordine).


	Se pot da ca parametru numarul de operatii de delete (./etapa2 testX.in 10).-> stergerea a 10 elemente de minim.
	In cazul in care numarul de operatii depaseste size-ul se va sterge pana la null.
	In cazul in care numarul de operatii nu este specificat se va efectua o singura stergere.

	Fisierele .out contin structurile afisate dupa fiecare stergere. (intai pentru treap, apoi pentru heap).
	Treapul afiseaza elementele pe linii in ordine crescatoare, iar heapul afiseaza vectorul cu ", " intre elemente.


	In makefile regula tests construieste testele cu numarul de stergeri scris. Se pot rula teste cu numar
de stergeri personalizate prin metodamentionata anterior in README.


Referinte:
https://www.tutorialspoint.com/cplusplus-program-to-implement-binary-heap -> documentatie heap
https://www.tutorialspoint.com/cplusplus-program-to-implement-treap       -> documentatie treap